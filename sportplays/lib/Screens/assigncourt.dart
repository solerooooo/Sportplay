import 'package:flutter/material.dart';

class AssignCourtPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Assign Court'),
      ),
      body: Center(
        child: Text('Assign Court Page'),
      ),
    );
  }
}
